# Login route (handled by security firewall)
api_auth_login:
    path: /{_locale}/auth/login
    methods: ['POST']
    requirements:
        _locale: 'en|fr|de|es|pt|nl'
    defaults:
        _locale: 'en'
